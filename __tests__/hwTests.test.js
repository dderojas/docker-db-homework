const { poolNew, poolOld } = require('../db')



describe('test', () => {
  beforeAll(async () => {
    
  })

  it('test', () => {
    let something = 'ha'
    let somethingElse = 'ha'
    expect(something).toEqual(somethingElse)
  })
})